aligned(8) class CompositionOffsetBox extends FullBox(ÅecttsÅf, version = 0, 0) {
unsigned int(32) entry_count;
int i;
for (i=0; i < entry_count; i++) {
unsigned int(32) sample_count;
unsigned int(32) sample_offset;
}
}